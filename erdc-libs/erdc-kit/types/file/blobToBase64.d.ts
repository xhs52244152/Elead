/**
 * blob 转 base64
 * @param {Blob} blob
 * @param {callback: (reader: FileReader) => void} [callback]
 * @returns {Promise<result: string>}
 */
export default function blobToBase64(blob: Blob, callback: any): Promise<result>;
