/**
 * 字符串转小驼峰
 * @param {string} string
 * @returns {string}
 * @example
 * ```javascript
 * camelize('foo-bar'); // => fooBar
 * camelize('foo_bar'); // => fooBar
 * camelize('foo.bar'); // => fooBar
 * ```
 */
export default function camelize(string: string): string;
